---
title: "Hello, conflicts from the other side"
format: html
editor: visual
---

```{r}
summary(cars)
str(cars)
library(ggplot2)
ggplot(cars, aes(x = speed, y = dist)) + labs (x = "mph", y = "ft") +
  geom_point() +
  theme_minimal()
```
 