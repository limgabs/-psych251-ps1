---
title: "hello, from the other side"
format: html
editor: visual
---

```{r}
summary(cars)
str(cars)
library(ggplot2)
ggplot(cars, aes(x = speed, y = dist)) +
  geom_point() +
  theme_minimal()
```
